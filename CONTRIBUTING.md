# Contributing

Run `just skills-sync`, install Node dependencies with `pnpm install --frozen-lockfile`, and run the checks described in README.md. Use the existing package lockfile and preserve third-party notices. Never commit credentials, private data or generated previews. Public interfaces need tests for missing and stale inputs as well as valid values.
