Read the root `AGENTS.md` and load the shared TypeScript, brand, and testing
skills. The website uses React and Vite. `just dev` runs the website;
`just storybook` runs component review; `just check` validates types, behavior
and the production build. Keep Storybook and website consumers on the same
components and released brand assets. Preserve the browser checks in
`.github/workflows/check.yml` and the private Buddy service binding.
