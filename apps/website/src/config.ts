/// The one place that knows the domain and inbox. A second
/// place would be a second thing to remember when one of them changes.
export const SITE = {
  url: "https://origin89.com",
  docsUrl: "https://docs.origin89.com",
  email: "hello@origin89.com",
} as const;
