dist/
design-dist/
.astro/
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.env
.env.production
.DS_Store
.idea/
.wrangler/
storybook-static/
storybook-server/
debug-storybook.log
storybook-report/
design-react-dist/
.website-server/
website-report/

# Build output. Everything under these is derived from @origin89/brand and
# from src/assets/, both of which are in the tree, so a fresh clone rebuilds
# them. The one exception is the equipment table, committed below, whose
# source is the private equipment research.
src/react/generated/*
!src/react/generated/catalogue.json
public/brand/
design/generated/

# Generated by scripts/deployment-config.mjs at deploy time.
wrangler.deploy.json
